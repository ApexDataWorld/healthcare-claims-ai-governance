# Quick Start

```bash
python -m pip install -r requirements.txt
python scripts/reproduce_all.py
python scripts/verify_outputs.py
```

The reproduction command creates a deterministic 1,200-claim synthetic dataset and regenerates the tables and figures used by the manuscript package.
